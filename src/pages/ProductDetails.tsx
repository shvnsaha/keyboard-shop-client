


const ProductDetails = () => {
    const { data, isLoading } = useGetSingleProductQuery(id);

    return (
        
        <div>
            product details
        </div>


    );
};

export default ProductDetails;